kind: Service
apiVersion: v1
metadata:
   name: backend-service
   namespace: invoicehub-ns

spec:
  selector:
     app: invoicehub-backend

  ports:
    - protocol: TCP
      port: 5219
      targetPort: 5219
    
  
type: NodePort